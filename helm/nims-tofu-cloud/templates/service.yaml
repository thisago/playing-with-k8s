apiVersion: v1
kind: Service
metadata:
  name: {{ include "nims-tofu-cloud.fullname" . }}
  labels:
    {{- include "nims-tofu-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "nims-tofu-cloud.selectorLabels" . | nindent 4 }}
