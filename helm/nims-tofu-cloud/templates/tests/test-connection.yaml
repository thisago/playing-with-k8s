apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nims-tofu-cloud.fullname" . }}-test-connection"
  labels:
    {{- include "nims-tofu-cloud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nims-tofu-cloud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
